#include <stdio.h>

/**
 * main - prints numbers between 0 and 9
 *
 * Return: Always 0 (this is successful)
 */
int main(void)
{
	int i;

	for (i = 48; i < 58; i++)
	{
		putchat(i);
	}
	putchar('\n');
	return (0);
}
